Drag-and-drop patch

Place the included file here in your repo:

/apps/legislate/app.js

It contains your provided calibration baked in and auto-seeds localStorage if empty (keys: legislate:path58, legislate:stages58). It won’t overwrite users’ existing saved calibration.
