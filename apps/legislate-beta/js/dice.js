import { Engine } from './engine.js'; export function setupDice(){ const btn=document.getElementById('roll-btn'); if(!btn) return;
  function tryRoll(){ if(Engine.busy||Engine.waiting) return; const n=1+Math.floor(Math.random()*6); btn.textContent='Roll ('+n+')'; Engine.afterRoll(n); }
  btn.onclick=tryRoll; window.addEventListener('keydown', e=>{ if(e.code==='Space'||e.code==='Enter'){ e.preventDefault(); tryRoll(); } }); }